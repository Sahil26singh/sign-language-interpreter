# Tested working combination on Python 3.11 (Windows).
# These versions are pinned deliberately - newer mediapipe/tensorflow releases
# have known incompatibilities with each other (protobuf version conflicts,
# and mediapipe's legacy mp.solutions API being removed in newer builds).

opencv-python==5.0.0.93
opencv-contrib-python==5.0.0.93
cvzone==2.0.0
mediapipe==0.10.14
tensorflow==2.17.0
numpy==1.26.4
protobuf==4.25.9
pyttsx3==2.99
keyboard==0.13.5